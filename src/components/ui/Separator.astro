---
import type { HTMLAttributes } from "astro/types"

type Props = HTMLAttributes<"hr">

const { class: className, ...props } = Astro.props
---

<hr aria-hidden="true" class:list={["w-full border-t-2 border-primary", className]} {...props} />
